@page "/table"

<h1>Personen Liste</h1>

<p>Dies ist eine Liste mit 2000 Eintr�gen welche sortiert werden kann</p>

@if (persons == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <PersonTable Persons="persons" SortedCallback="sort"></PersonTable>
}
