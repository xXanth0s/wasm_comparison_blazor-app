@page "/quicksortstring/{Count:int}/{AutoStart:bool}/{RunCount:int}/{IsJs:bool}"


<div class="container">
    <h1> Quicksort mit Strings</h1>
    <h4>Hier wird gemessen, wie lange der Quicksort Algorithmus zum sortieren von @Count Einträgen benötigt</h4>
    @if (_sortableDataArray != null)
    {
<div>
    <button @onclick="Sort" disabled="@_isRunning" class="btn btn-primary">Start With WebAssembly</button>
    <button @onclick="SortJS" disabled="@_isRunning" class="btn btn-primary ml-2">Start With JavaScript</button>
    @if (_isRunning)
    {
        <p>Quicksort wird momentan ausgeführt. Bitte warten</p>
    }
    @if (_isFinished)
    {
        <div v-if="showResult" class="card">
            <div class="card-body">
                <p>Quicksort wurde beendet</p>
                <p>Benötigte Zeit @_elapsedTime</p>
            </div>

            <div class="card-body">
                <p>Vorschau der ersten 1000 Elemente:</p>

                @foreach (var entry in _preview)
                {
                    <p>
                        <code>@entry</code>
                    </p>
                }
            </div>
        </div>
    }
</div>
    }
    else
    {
        <div>
            <div class="spinner-container">
                <div class="spinner-border" role="status">
                    <span class="sr-only">Loading...</span>
                </div>
            </div>
        </div>
    }
</div>